# Wake-word spotter engines. Both are lightweight and torch-free at runtime:
#   livekit-wakeword (default) — custom ONNX "James" model + bundled feature
#     models; onnxruntime only.
#   vosk (fallback engine) — offline ASR constrained to a keyword grammar.
# numpy is used by the livekit engine and selftest.py.
livekit-wakeword
vosk
numpy
