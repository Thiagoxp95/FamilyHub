# Wake-word + speaker-lock engines. All torch-free at runtime:
#   livekit-wakeword (default wake engine) — custom ONNX "James" model.
#   vosk (fallback wake engine) — offline ASR constrained to a keyword grammar.
#   sherpa-onnx — silero VAD + speaker-embedding for the speaker hard-lock gate.
# numpy is used by the livekit engine, the gate, and selftest.py.
livekit-wakeword
vosk
sherpa-onnx
numpy
