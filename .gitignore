node_modules
out
release
dist
.turbo
.DS_Store
*.log
coverage
.env
.env.local
.env.*.local
.credentials
sidecar/.venv/
# Self-contained Python runtime bundled into packaged builds (built by
# apps/electron/scripts/build-sidecar-runtime.sh; ~200 MB).
sidecar/.runtime/
# Ignore backup/scratch models, but keep the committed wake classifier
# (livekit-wakeword conv-attention ONNX).
sidecar/models/*
!sidecar/models/hey_james.onnx
__pycache__/
*.pyc

# Wake-word training working area (large, off-device artifacts; ~20 GB).
# Keep the reproducibility recipe (training/README.md), config, and runner.
sidecar/training/.venv/
sidecar/training/data/
sidecar/training/my_custom_model/
sidecar/training/smoke_model/
sidecar/training/piper-sample-generator/

# Stray bundled main-process build artifact (real entry is apps/electron/out/main/index.js)
/index.js
