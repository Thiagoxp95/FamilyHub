{
  "name": "@family-hub/electron",
  "version": "0.0.13",
  "private": true,
  "type": "module",
  "main": "./out/main/index.js",
  "scripts": {
    "build": "npm run typecheck && electron-vite build",
    "build:sidecar-runtime": "bash scripts/build-sidecar-runtime.sh",
    "dev": "electron-vite dev",
    "dev:packed": "npm run package && open release/mac-arm64/FamilyHub.app",
    "dist": "npm run build && npm run build:sidecar-runtime && electron-builder",
    "dist:mac:arm64": "npm run build && npm run build:sidecar-runtime && electron-builder --mac --arm64",
    "dmg": "npm run build && npm run build:sidecar-runtime && electron-builder --mac dmg --arm64 --publish never",
    "release:mac:arm64": "npm run build && npm run build:sidecar-runtime && electron-builder --mac --arm64 --publish always",
    "release": "bash scripts/release.sh",
    "ship": "bash scripts/ship.sh",
    "setup-signing": "bash scripts/setup-signing.sh",
    "lint": "eslint . --max-warnings=0",
    "package": "npm run build && npm run build:sidecar-runtime && electron-builder --dir",
    "preview": "electron-vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.node.json && tsc --noEmit -p tsconfig.web.json"
  },
  "dependencies": {
    "@google-cloud/speech": "7.3.1",
    "@google/genai": "2.8.0",
    "dotenv": "16.6.1",
    "electron-updater": "6.8.9",
    "react": "19.2.6",
    "react-dom": "19.2.6"
  },
  "devDependencies": {
    "@swc/core": "1.15.40",
    "@types/node": "25.9.1",
    "@types/react": "19.2.15",
    "@types/react-dom": "19.2.3",
    "@vitejs/plugin-react": "5.2.0",
    "electron": "42.2.0",
    "electron-builder": "26.8.1",
    "electron-vite": "5.0.0",
    "vite": "7.3.3",
    "vitest": "4.1.8"
  },
  "build": {
    "appId": "com.familyhub.app",
    "productName": "FamilyHub",
    "artifactName": "${productName}-${version}-${arch}.${ext}",
    "directories": {
      "output": "release"
    },
    "files": [
      "out/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "../../sidecar",
        "to": "sidecar",
        "filter": [
          "**/*",
          "!.venv/**",
          "!training/**",
          "!**/.git",
          "!**/.git/**",
          "!**/__pycache__/**",
          "!**/*.pyc"
        ]
      }
    ],
    "mac": {
      "category": "public.app-category.lifestyle",
      "entitlements": "build/entitlements.mac.plist",
      "entitlementsInherit": "build/entitlements.mac.plist",
      "extendInfo": {
        "NSAppleEventsUsageDescription": "FamilyHub uses Automation to read your Calendar and Reminders for the kitchen dashboard.",
        "NSMicrophoneUsageDescription": "FamilyHub uses the microphone to listen for the James wake word and assistant commands."
      },
      "hardenedRuntime": true,
      "notarize": false,
      "identity": "FamilyHub Self Signed",
      "target": [
        {
          "target": "dmg",
          "arch": [
            "arm64"
          ]
        },
        {
          "target": "zip",
          "arch": [
            "arm64"
          ]
        }
      ]
    },
    "publish": [
      {
        "provider": "github",
        "owner": "Thiagoxp95",
        "repo": "FamilyHub",
        "releaseType": "release"
      }
    ],
    "win": {
      "target": "nsis"
    },
    "linux": {
      "target": "AppImage"
    }
  }
}
